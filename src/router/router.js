import All from '../components/';

const routes = [
    { path: '/', component: All.SelfAdaption },
    { path: '/triangle', component: All.Triangle},
    { path: '/table', component: All.Table },
    { path: '/comment', component: All.Comment },
];

export default routes;
